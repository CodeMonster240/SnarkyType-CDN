# Basically only for storing our assets because I'm too lazy to upload it :)

# 🚀 Quick Start

Type the command `npx http-server -c-1 --cors` to start server!
All other stuff is in SnarkyType repo
